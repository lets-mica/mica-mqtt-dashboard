# 认证机制实现计划（Basic 默认 + 可配置 OAuth）

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为 mica-mqtt-dashboard 实现可配置认证：默认 Basic（现有行为收拢到 AuthProvider 策略），可选通用 OIDC + PKCE 的 OAuth 登录门禁，通过 `VITE_AUTH_MODE` 环境变量构建时切换。

**架构：** 新增 `src/auth/` 目录定义 `AuthProvider` 接口（`types.ts`），实现 `BasicAuthProvider`（`basic.ts`）与 `OidcAuthProvider`（`oidc.ts`），`index.ts` 工厂按环境变量返回实例。Pinia `auth` store 持有 Provider 实例并暴露统一 API；axios 拦截器通过注入的凭据获取函数取 broker 凭据（basic 模式取 localStorage 账密，oauth 模式取 `VITE_BROKER_*`）。

**技术栈：** Vue 3 + TypeScript + Pinia + Axios + Element Plus。OAuth 用原生 `crypto`（PKCE + SHA-256），不新增依赖。

**规格文档：** `docs/superpowers/specs/2026-08-06-auth-design.md`

**验证方式（项目无测试框架，AGENTS.md 规定）：** 每任务以 `pnpm type-check`（vue-tsc --noEmit）与 `pnpm lint:check`（eslint）验证，最终手动验证清单见任务 9。**不要运行 `pnpm test`。**

---

## 文件结构

| 文件 | 操作 | 职责 |
|---|---|---|
| `src/auth/types.ts` | 创建 | `AuthMode`、`BasicCredentials`、`LoginResult`、`AuthProvider` 接口 |
| `src/auth/basic.ts` | 创建 | `BasicAuthProvider`：账密登录验证 + localStorage 会话 |
| `src/auth/oidc.ts` | 创建 | `OidcAuthProvider`：OIDC 发现 + PKCE 授权码流程 + sessionStorage 会话 |
| `src/auth/index.ts` | 创建 | `createAuthProvider()` 工厂 + 配置校验 + 类型重导出 |
| `src/vite-env.d.ts` | 修改 | 声明新增的 `ImportMetaEnv` 字段 |
| `src/stores/auth.ts` | 重写 | 持有 Provider 实例，向组件/守卫暴露统一状态与方法 |
| `src/utils/api.ts` | 修改 | 凭据来源改为注入函数（消除与 store 的循环依赖），401 处理走注入的登出回调 |
| `src/main.ts` | 修改 | 初始化后注入 `credentialsProvider` 与 `unauthorizedHandler` |
| `src/views/Login.vue` | 修改 | 按 `authStore.mode` 分支：basic 表单 / oauth 按钮 + 回调处理 |
| `.env.development` | 修改 | 新增认证相关环境变量 |
| `.env.production` | 修改 | 新增认证相关环境变量 |

> 注意：`src/utils/api.ts` 当前直接读 `localStorage('mqtt_username'/'mqtt_password')` 附加 Basic 凭据；为支持 oauth 模式（凭据来自 env）并避免 `api.ts` 与 `stores/auth.ts` 相互 import 的循环依赖，改为**注入模式**：`main.ts` 在 pinia 安装后调用 `setCredentialsProvider(() => useAuthStore().getApiCredentials())` 与 `setUnauthorizedHandler(() => useAuthStore().logout())`。

---

### 任务 1：`src/auth/types.ts` — 类型与接口定义

**文件：**
- 创建：`src/auth/types.ts`

- [ ] **步骤 1：编写文件**

```ts
// src/auth/types.ts
export type AuthMode = 'basic' | 'oauth'

export interface BasicCredentials {
  username: string
  password: string
}

export interface LoginResult {
  success: boolean
  message?: string
}

export interface AuthProvider {
  readonly mode: AuthMode
  init(): void
  isAuthenticated(): boolean
  getUsername(): string
  login(username: string, password: string): Promise<LoginResult>
  startLogin(): Promise<void>
  handleCallback(): Promise<LoginResult>
  getApiCredentials(): BasicCredentials | null
  logout(): void
}
```

- [ ] **步骤 2：验证**

运行：`pnpm type-check`
预期：PASS（无类型错误；`src/auth/types.ts` 当前无引用，仅新增声明）

- [ ] **步骤 3：Commit**

```bash
git add src/auth/types.ts
git commit -m "feat(auth): 定义 AuthProvider 接口与认证类型"
```

---

### 任务 2：`src/auth/basic.ts` — BasicAuthProvider

**文件：**
- 创建：`src/auth/basic.ts`

- [ ] **步骤 1：编写文件**

```ts
// src/auth/basic.ts
import { api } from '@/utils/api'
import type { AuthProvider, BasicCredentials, LoginResult } from './types'

const USERNAME_KEY = 'mqtt_username'
const PASSWORD_KEY = 'mqtt_password'

export function createBasicAuthProvider(): AuthProvider {
  let isAuthenticated = false
  let username = ''
  let password = ''

  const init = () => {
    const storedUsername = localStorage.getItem(USERNAME_KEY)
    const storedPassword = localStorage.getItem(PASSWORD_KEY)
    if (storedUsername && storedPassword) {
      username = storedUsername
      password = storedPassword
      isAuthenticated = true
    }
  }

  const login = async (user: string, pass: string): Promise<LoginResult> => {
    try {
      const response = await api.get('/api/v1/stats', {
        auth: { username: user, password: pass }
      })
      if (response.status === 200) {
        username = user
        password = pass
        isAuthenticated = true
        localStorage.setItem(USERNAME_KEY, user)
        localStorage.setItem(PASSWORD_KEY, pass)
        return { success: true }
      }
      return { success: false, message: '认证失败' }
    } catch (error: any) {
      if (error.response?.status === 401) {
        return { success: false, message: '用户名或密码错误' }
      }
      return { success: false, message: error.message || '登录失败，请检查服务器连接' }
    }
  }

  const getApiCredentials = (): BasicCredentials | null =>
    isAuthenticated ? { username, password } : null

  const logout = () => {
    username = ''
    password = ''
    isAuthenticated = false
    localStorage.removeItem(USERNAME_KEY)
    localStorage.removeItem(PASSWORD_KEY)
  }

  return {
    mode: 'basic',
    init,
    isAuthenticated: () => isAuthenticated,
    getUsername: () => username,
    login,
    startLogin: async () => {
      throw new Error('basic 模式不支持 OAuth 登录')
    },
    handleCallback: async (): Promise<LoginResult> => ({
      success: false,
      message: 'basic 模式不支持 OAuth 回调'
    }),
    getApiCredentials,
    logout
  }
}
```

- [ ] **步骤 2：验证**

运行：`pnpm type-check`
预期：PASS（若报 `api` 未使用/模块解析错误，说明 `src/utils/api.ts` 尚在，无碍；本文件引用 `@/utils/api` 已有实现）

- [ ] **步骤 3：Commit**

```bash
git add src/auth/basic.ts
git commit -m "feat(auth): 实现 BasicAuthProvider"
```

---

### 任务 3：`src/auth/oidc.ts` — OidcAuthProvider（OIDC + PKCE）

**文件：**
- 创建：`src/auth/oidc.ts`

- [ ] **步骤 1：编写文件**

```ts
// src/auth/oidc.ts
import type { AuthProvider, BasicCredentials, LoginResult } from './types'

const VERIFIER_KEY = 'mqtt_oauth_verifier'
const STATE_KEY = 'mqtt_oauth_state'
const TOKEN_KEY = 'mqtt_oauth_token'
const USERNAME_KEY = 'mqtt_oauth_username'

export interface OidcConfig {
  issuer: string
  clientId: string
  scopes: string[]
  brokerUsername: string
  brokerPassword: string
}

interface OidcEndpoints {
  authorizationEndpoint: string
  tokenEndpoint: string
  userinfoEndpoint: string
}

const base64UrlEncode = (input: ArrayBuffer): string => {
  const bytes = new Uint8Array(input)
  let binary = ''
  bytes.forEach((byte) => {
    binary += String.fromCharCode(byte)
  })
  return btoa(binary).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '')
}

const generateVerifier = (): string => {
  const bytes = new Uint8Array(32)
  crypto.getRandomValues(bytes)
  return base64UrlEncode(bytes.buffer)
}

const generateChallenge = async (verifier: string): Promise<string> => {
  const digest = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(verifier))
  return base64UrlEncode(digest)
}

const getRedirectUri = (): string => `${window.location.origin}/login`

const discoverEndpoints = async (issuer: string): Promise<OidcEndpoints> => {
  const base = issuer.replace(/\/+$/, '')
  const response = await fetch(`${base}/.well-known/openid-configuration`)
  if (!response.ok) {
    throw new Error(`OIDC 配置发现失败: HTTP ${response.status}`)
  }
  const config = await response.json()
  return {
    authorizationEndpoint: config.authorization_endpoint,
    tokenEndpoint: config.token_endpoint,
    userinfoEndpoint: config.userinfo_endpoint
  }
}

export function createOidcAuthProvider(config: OidcConfig): AuthProvider {
  let isAuthenticated = false
  let username = ''
  let token = ''

  const init = () => {
    const storedToken = sessionStorage.getItem(TOKEN_KEY)
    const storedUsername = sessionStorage.getItem(USERNAME_KEY)
    if (storedToken && storedUsername) {
      token = storedToken
      username = storedUsername
      isAuthenticated = true
    }
  }

  const startLogin = async (): Promise<void> => {
    const verifier = generateVerifier()
    const challenge = await generateChallenge(verifier)
    const state = crypto.randomUUID()
    sessionStorage.setItem(VERIFIER_KEY, verifier)
    sessionStorage.setItem(STATE_KEY, state)

    const { authorizationEndpoint } = await discoverEndpoints(config.issuer)
    const params = new URLSearchParams({
      response_type: 'code',
      client_id: config.clientId,
      redirect_uri: getRedirectUri(),
      scope: config.scopes.join(' '),
      state,
      code_challenge: challenge,
      code_challenge_method: 'S256'
    })
    window.location.href = `${authorizationEndpoint}?${params.toString()}`
  }

  const handleCallback = async (): Promise<LoginResult> => {
    const url = new URL(window.location.href)
    const code = url.searchParams.get('code')
    const state = url.searchParams.get('state')
    const storedState = sessionStorage.getItem(STATE_KEY)

    if (!code || !state || !storedState || state !== storedState) {
      return { success: false, message: 'OAuth 回调校验失败' }
    }

    const verifier = sessionStorage.getItem(VERIFIER_KEY)
    if (!verifier) {
      return { success: false, message: '缺少 code_verifier' }
    }

    try {
      const { tokenEndpoint, userinfoEndpoint } = await discoverEndpoints(config.issuer)
      const body = new URLSearchParams({
        grant_type: 'authorization_code',
        code,
        redirect_uri: getRedirectUri(),
        client_id: config.clientId,
        code_verifier: verifier
      })
      const tokenResponse = await fetch(tokenEndpoint, {
        method: 'POST',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
        body
      })
      if (!tokenResponse.ok) {
        return { success: false, message: '换取 token 失败' }
      }
      const tokenData = await tokenResponse.json()
      token = tokenData.access_token as string

      const userResponse = await fetch(userinfoEndpoint, {
        headers: { Authorization: `Bearer ${token}` }
      })
      if (!userResponse.ok) {
        return { success: false, message: '获取用户信息失败' }
      }
      const user = await userResponse.json()
      username = user.preferred_username || user.sub || ''

      sessionStorage.setItem(TOKEN_KEY, token)
      sessionStorage.setItem(USERNAME_KEY, username)
      isAuthenticated = true
      return { success: true }
    } catch (error: any) {
      return { success: false, message: error.message || 'OAuth 登录失败' }
    } finally {
      sessionStorage.removeItem(VERIFIER_KEY)
      sessionStorage.removeItem(STATE_KEY)
    }
  }

  const getApiCredentials = (): BasicCredentials | null =>
    isAuthenticated
      ? { username: config.brokerUsername, password: config.brokerPassword }
      : null

  const logout = () => {
    isAuthenticated = false
    token = ''
    username = ''
    sessionStorage.removeItem(TOKEN_KEY)
    sessionStorage.removeItem(USERNAME_KEY)
  }

  return {
    mode: 'oauth',
    init,
    isAuthenticated: () => isAuthenticated,
    getUsername: () => username,
    login: async (): Promise<LoginResult> => ({
      success: false,
      message: 'oauth 模式请使用企业账号登录'
    }),
    startLogin,
    handleCallback,
    getApiCredentials,
    logout
  }
}
```

- [ ] **步骤 2：验证**

运行：`pnpm type-check`
预期：PASS（`token` 赋值后未读会有 `noUnusedLocals` 风险——若 tsconfig 开启，改为仅保留必要字段或确认不报错；本项目现有代码含类似模式，以实际输出为准，若报 unused 则在 `logout` 中已使用 `token = ''`，可消除告警）

- [ ] **步骤 3：Commit**

```bash
git add src/auth/oidc.ts
git commit -m "feat(auth): 实现 OidcAuthProvider（OIDC + PKCE）"
```

---

### 任务 4：`src/auth/index.ts` — 工厂 + 配置校验 + 环境变量类型

**文件：**
- 创建：`src/auth/index.ts`
- 修改：`src/vite-env.d.ts`

- [ ] **步骤 1：编写工厂文件**

```ts
// src/auth/index.ts
import { createBasicAuthProvider } from './basic'
import { createOidcAuthProvider } from './oidc'
import type { AuthMode, AuthProvider } from './types'

export * from './types'

export function createAuthProvider(): AuthProvider {
  const mode = (import.meta.env.VITE_AUTH_MODE || 'basic') as AuthMode

  if (mode === 'oauth') {
    const issuer = import.meta.env.VITE_OAUTH_ISSUER
    const clientId = import.meta.env.VITE_OAUTH_CLIENT_ID
    if (!issuer || !clientId) {
      throw new Error('OAuth 模式缺少配置: VITE_OAUTH_ISSUER / VITE_OAUTH_CLIENT_ID')
    }
    return createOidcAuthProvider({
      issuer,
      clientId,
      scopes: (import.meta.env.VITE_OAUTH_SCOPES || 'openid profile').split(' '),
      brokerUsername: import.meta.env.VITE_BROKER_USERNAME || '',
      brokerPassword: import.meta.env.VITE_BROKER_PASSWORD || ''
    })
  }

  return createBasicAuthProvider()
}
```

- [ ] **步骤 2：更新环境变量类型声明**

`src/vite-env.d.ts` 完整替换为：

```ts
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_API_BASE_URL: string
  readonly VITE_MQTT_HOST: string
  readonly VITE_MQTT_PORT: string
  readonly VITE_MQTT_CLIENT_ID_PREFIX: string
  readonly VITE_AUTH_MODE: string
  readonly VITE_OAUTH_ISSUER: string
  readonly VITE_OAUTH_CLIENT_ID: string
  readonly VITE_OAUTH_SCOPES: string
  readonly VITE_BROKER_USERNAME: string
  readonly VITE_BROKER_PASSWORD: string
}

interface ImportMeta {
  readonly env: ImportMetaEnv
}
```

- [ ] **步骤 3：验证**

运行：`pnpm type-check`
预期：PASS（`import.meta.env.VITE_AUTH_MODE` 等新字段有类型声明；`createAuthProvider` 被 `stores/auth.ts` 引用前无未使用告警——若 eslint 报"模块导出未使用"，属正常，store 改造后消除）

- [ ] **步骤 4：Commit**

```bash
git add src/auth/index.ts src/vite-env.d.ts
git commit -m "feat(auth): 新增认证工厂与环境变量类型声明"
```

---

### 任务 5：重写 `src/stores/auth.ts` — 持有 Provider 实例

**文件：**
- 重写：`src/stores/auth.ts`

- [ ] **步骤 1：重写文件**

```ts
// src/stores/auth.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { api } from '@/utils/api'
import { createAuthProvider } from '@/auth'
import type { AuthProvider, BasicCredentials, LoginResult } from '@/auth'

export const useAuthStore = defineStore('auth', () => {
  const provider: AuthProvider = createAuthProvider()
  const mode = ref(provider.mode)
  const isAuthenticated = ref(provider.isAuthenticated())
  const username = ref(provider.getUsername())

  const syncState = () => {
    isAuthenticated.value = provider.isAuthenticated()
    username.value = provider.getUsername()
  }

  const initAuth = () => {
    provider.init()
    syncState()
  }

  const login = async (user: string, pass: string): Promise<LoginResult> => {
    const result = await provider.login(user, pass)
    syncState()
    return result
  }

  const startLogin = async (): Promise<void> => {
    await provider.startLogin()
  }

  const handleCallback = async (): Promise<LoginResult> => {
    const result = await provider.handleCallback()
    syncState()
    return result
  }

  const getApiCredentials = (): BasicCredentials | null => provider.getApiCredentials()

  const logout = () => {
    provider.logout()
    syncState()
  }

  const checkAuth = async (): Promise<boolean> => {
    if (!isAuthenticated.value) return false

    try {
      const response = await api.get('/api/v1/stats')
      const valid = response.status === 200
      if (!valid) {
        logout()
      }
      return valid
    } catch (error: any) {
      if (error.response?.status === 401) {
        logout()
        return false
      }
      return isAuthenticated.value
    }
  }

  return {
    mode,
    isAuthenticated,
    username,
    initAuth,
    login,
    startLogin,
    handleCallback,
    getApiCredentials,
    logout,
    checkAuth
  }
})
```

- [ ] **步骤 2：验证**

运行：`pnpm type-check`
预期：PASS（`router/index.ts` 与 `Layout.vue`、`Login.vue` 仍调用 `authStore.login/logout/isAuthenticated`，接口未变）

- [ ] **步骤 3：Commit**

```bash
git add src/stores/auth.ts
git commit -m "refactor(auth): store 改为持有 AuthProvider 实例"
```

---

### 任务 6：修改 `src/utils/api.ts` — 注入式凭据与 401 处理

**文件：**
- 修改：`src/utils/api.ts`

- [ ] **步骤 1：修改文件**

`src/utils/api.ts` 完整替换为：

```ts
// src/utils/api.ts
import axios from 'axios'
import { ElMessage } from 'element-plus'
import type { BasicCredentials } from '@/auth'

// 认证凭据获取函数（由 main.ts 注入，避免与 auth store 循环依赖）
let credentialsProvider: (() => BasicCredentials | null) | null = null
// 认证失效回调（由 main.ts 注入，调用 authStore.logout）
let unauthorizedHandler: (() => void) | null = null

export const setCredentialsProvider = (fn: (() => BasicCredentials | null) | null) => {
  credentialsProvider = fn
}

export const setUnauthorizedHandler = (fn: (() => void) | null) => {
  unauthorizedHandler = fn
}

// 创建axios实例
const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
api.interceptors.request.use(
  (config) => {
    const credentials = credentialsProvider ? credentialsProvider() : null
    if (credentials) {
      config.auth = credentials
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
api.interceptors.response.use(
  (response) => {
    return response
  },
  (error) => {
    if (error.response) {
      const { status, data } = error.response

      switch (status) {
        case 401:
          if (unauthorizedHandler) {
            unauthorizedHandler()
          }
          // 避免在登录页面重复提示
          if (!window.location.pathname.includes('/login')) {
            ElMessage.error('认证失败，请重新登录')

            // 延迟跳转，确保消息显示
            setTimeout(() => {
              const currentPath = window.location.pathname
              window.location.href = `/login?redirect=${encodeURIComponent(currentPath)}`
            }, 500)
          }
          break
        case 404:
          ElMessage.error('请求的资源不存在')
          break
        case 500:
          ElMessage.error('服务器内部错误')
          break
        default:
          ElMessage.error(data?.message || '请求失败')
      }
    } else if (error.request) {
      ElMessage.error('网络连接失败，请检查服务器状态')
    } else {
      ElMessage.error('请求配置错误')
    }

    return Promise.reject(error)
  }
)

export { api }
```

> 变更说明：401 分支不再直接操作 `localStorage`（basic 模式由 `BasicAuthProvider.logout` 清 localStorage；oauth 模式由 `OidcAuthProvider.logout` 清 sessionStorage），统一走注入的 `unauthorizedHandler`。

- [ ] **步骤 2：验证**

运行：`pnpm type-check`
预期：PASS（`setCredentialsProvider`/`setUnauthorizedHandler` 导出暂未使用，eslint 可能报"已导出但未在模块内使用"——导出函数不触发该规则；若报错，以 `pnpm lint:check` 输出为准修正）

- [ ] **步骤 3：Commit**

```bash
git add src/utils/api.ts
git commit -m "refactor(api): 凭据与 401 处理改为注入式，支持多认证模式"
```

---

### 任务 7：修改 `src/main.ts` — 注入凭据与登出回调

**文件：**
- 修改：`src/main.ts`

- [ ] **步骤 1：修改文件**

`src/main.ts` 完整替换为：

```ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import 'element-plus/theme-chalk/dark/css-vars.css'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'
import zhCn from 'element-plus/dist/locale/zh-cn.mjs'

import App from './App.vue'
import router from './router'
import { setCredentialsProvider, setUnauthorizedHandler } from './utils/api'
import { useAuthStore } from './stores/auth'

const app = createApp(App)

// 注册所有图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

const pinia = createPinia()
app.use(pinia)

// 注入认证凭据获取与失效处理（依赖 auth store，需在 pinia 安装后）
setCredentialsProvider(() => useAuthStore().getApiCredentials())
setUnauthorizedHandler(() => useAuthStore().logout())

app.use(router)
app.use(ElementPlus, {
  locale: zhCn
})

app.mount('#app')
```

- [ ] **步骤 2：验证**

运行：`pnpm type-check`
预期：PASS（`main.ts` 在 `useAuthStore()` 前已 `app.use(pinia)`；无未使用导入）

- [ ] **步骤 3：Commit**

```bash
git add src/main.ts
git commit -m "feat(auth): main.ts 注入凭据与登出回调"
```

---

### 任务 8：修改 `src/views/Login.vue` — 按模式分支渲染

**文件：**
- 修改：`src/views/Login.vue`

- [ ] **步骤 1：修改模板与脚本**

`src/views/Login.vue` 完整替换为：

```vue
<template>
  <div class="login-container">
    <div class="login-box">
      <div class="login-header">
        <div class="logo">
          <el-icon :size="48" color="#409EFF"><Connection /></el-icon>
        </div>
        <h1>MICA MQTT</h1>
        <p class="subtitle">管理控制台</p>
      </div>

      <!-- Basic 模式：账密表单 -->
      <template v-if="!isOAuth">
        <el-form
          ref="loginFormRef"
          :model="loginForm"
          :rules="rules"
          class="login-form"
          @submit.prevent="handleLogin"
        >
          <el-form-item prop="username">
            <el-input
              v-model="loginForm.username"
              placeholder="用户名"
              size="large"
              clearable
              @keyup.enter="handleLogin"
            >
              <template #prefix>
                <el-icon><User /></el-icon>
              </template>
            </el-input>
          </el-form-item>

          <el-form-item prop="password">
            <el-input
              v-model="loginForm.password"
              type="password"
              placeholder="密码"
              size="large"
              show-password
              @keyup.enter="handleLogin"
            >
              <template #prefix>
                <el-icon><Lock /></el-icon>
              </template>
            </el-input>
          </el-form-item>

          <el-form-item>
            <el-checkbox v-model="loginForm.remember">记住密码</el-checkbox>
          </el-form-item>

          <el-form-item>
            <el-button
              type="primary"
              size="large"
              :loading="loading"
              class="login-button"
              @click="handleLogin"
            >
              登录
            </el-button>
          </el-form-item>
        </el-form>
      </template>

      <!-- OAuth 模式：企业账号登录 -->
      <template v-else>
        <el-button
          type="primary"
          size="large"
          :loading="oauthLoading"
          class="login-button oauth-button"
          @click="handleOAuthLogin"
        >
          <el-icon><Connection /></el-icon>
          使用企业账号登录
        </el-button>
      </template>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { useRouter, useRoute } from 'vue-router'
import { ElMessage } from 'element-plus'
import { Connection, User, Lock } from '@element-plus/icons-vue'
import { useAuthStore } from '@/stores/auth'
import type { FormInstance, FormRules } from 'element-plus'

const router = useRouter()
const route = useRoute()
const authStore = useAuthStore()

const isOAuth = authStore.mode === 'oauth'
const loginFormRef = ref<FormInstance>()
const loading = ref(false)
const oauthLoading = ref(false)

const loginForm = reactive({
  username: '',
  password: '',
  remember: true
})

const rules: FormRules = {
  username: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 3, message: '密码长度至少3位', trigger: 'blur' }
  ]
}

onMounted(async () => {
  if (isOAuth) {
    // 处理 IdP 授权回调（redirect_uri 为 /login）
    const url = new URL(window.location.href)
    if (url.searchParams.has('code') || url.searchParams.has('state')) {
      oauthLoading.value = true
      try {
        const result = await authStore.handleCallback()
        if (result.success) {
          ElMessage.success('登录成功')
          const redirect = route.query.redirect as string
          await router.push(redirect || '/dashboard')
        } else {
          ElMessage.error(result.message || 'OAuth 登录失败')
        }
      } finally {
        oauthLoading.value = false
      }
    }
    return
  }

  // Basic 模式：从本地存储恢复记住的账号
  const savedUsername = localStorage.getItem('saved_username')
  if (savedUsername) {
    loginForm.username = savedUsername
    loginForm.remember = true
  }
})

const handleLogin = async () => {
  if (!loginFormRef.value) return

  await loginFormRef.value.validate(async (valid) => {
    if (!valid) return

    loading.value = true

    try {
      const result = await authStore.login(loginForm.username, loginForm.password)

      if (result.success) {
        ElMessage.success('登录成功')

        // 保存/清除记住的密码
        if (loginForm.remember) {
          localStorage.setItem('saved_username', loginForm.username)
        } else {
          localStorage.removeItem('saved_username')
        }

        // 跳转到目标页面或首页
        const redirect = route.query.redirect as string
        await router.push(redirect || '/dashboard')
      } else {
        ElMessage.error(result.message || '登录失败')
      }
    } catch (error: any) {
      ElMessage.error(error.message || '登录失败')
    } finally {
      loading.value = false
    }
  })
}

const handleOAuthLogin = async () => {
  oauthLoading.value = true
  try {
    // 跳转 IdP，成功后由 IdP 回跳 /login 触发回调处理
    await authStore.startLogin()
  } catch (error: any) {
    oauthLoading.value = false
    ElMessage.error(error.message || 'OAuth 登录失败')
  }
}
</script>

<style scoped>
.login-container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.login-box {
  width: 420px;
  padding: 40px;
  background: white;
  border-radius: 12px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

.login-header {
  text-align: center;
  margin-bottom: 40px;
}

.logo {
  margin-bottom: 16px;
}

.login-header h1 {
  font-size: 32px;
  font-weight: bold;
  color: #333;
  margin: 0 0 8px 0;
}

.subtitle {
  font-size: 14px;
  color: #999;
  margin: 0;
}

.login-form {
  margin-top: 32px;
}

.login-button {
  width: 100%;
}

.oauth-button {
  margin-top: 32px;
}

:deep(.el-form-item) {
  margin-bottom: 24px;
}

:deep(.el-input__wrapper) {
  padding: 12px 16px;
}
</style>
```

- [ ] **步骤 2：验证**

运行：`pnpm type-check`
预期：PASS（`authStore.mode`、`startLogin`、`handleCallback` 均在任务 5 的 store 中已暴露）

- [ ] **步骤 3：Commit**

```bash
git add src/views/Login.vue
git commit -m "feat(auth): 登录页支持 Basic/OAuth 双模式"
```

---

### 任务 9：更新环境变量文件

**文件：**
- 修改：`.env.development`
- 修改：`.env.production`

- [ ] **步骤 1：修改 `.env.development`**

完整替换为：

```env
# Mica MQTT Dashboard 开发环境配置

# API 配置
VITE_API_BASE_URL=

# 认证模式: basic（默认） | oauth
VITE_AUTH_MODE=basic

# OAuth 模式配置（VITE_AUTH_MODE=oauth 时生效）
VITE_OAUTH_ISSUER=https://sso.example.com
VITE_OAUTH_CLIENT_ID=your-client-id
VITE_OAUTH_SCOPES=openid profile

# broker 凭据（oauth 模式必填；basic 模式使用用户输入的账密）
VITE_BROKER_USERNAME=
VITE_BROKER_PASSWORD=

# MQTT 连接配置
VITE_MQTT_HOST=localhost
VITE_MQTT_PORT=8083
VITE_MQTT_CLIENT_ID_PREFIX=mica_web_
```

- [ ] **步骤 2：修改 `.env.production`**

完整替换为：

```env
# Mica MQTT Dashboard 环境配置

# API 配置
VITE_API_BASE_URL=

# 认证模式: basic（默认） | oauth
VITE_AUTH_MODE=basic

# OAuth 模式配置（VITE_AUTH_MODE=oauth 时生效）
VITE_OAUTH_ISSUER=https://sso.example.com
VITE_OAUTH_CLIENT_ID=your-client-id
VITE_OAUTH_SCOPES=openid profile

# broker 凭据（oauth 模式必填；basic 模式使用用户输入的账密）
VITE_BROKER_USERNAME=
VITE_BROKER_PASSWORD=

# MQTT 连接配置
VITE_MQTT_HOST=localhost
VITE_MQTT_PORT=8083
VITE_MQTT_CLIENT_ID_PREFIX=mica_web_
```

- [ ] **步骤 3：验证**

运行：`pnpm type-check`
预期：PASS（环境变量仅字符串替换，不影响类型）

- [ ] **步骤 4：Commit**

```bash
git add .env.development .env.production
git commit -m "feat(auth): 新增认证模式与 OAuth 环境变量配置"
```

---

### 任务 10：全量验证与回归

**文件：**
- 无代码改动，仅验证

- [ ] **步骤 1：类型检查**

运行：`pnpm type-check`
预期：PASS，无类型错误

- [ ] **步骤 2：Lint 检查**

运行：`pnpm lint:check`
预期：PASS，无 error（若出现格式/规则告警，按输出修复后重跑）

- [ ] **步骤 3：构建验证**

运行：`pnpm build`
预期：PASS，产出 `dist/`

- [ ] **步骤 4：手动验证清单（Basic 回归）**

1. `VITE_AUTH_MODE=basic`（默认）启动 `pnpm dev`
2. 登录页显示账密表单 → 输入正确账密 → 登录成功跳 `/dashboard`
3. 顶栏显示用户名；退出登录 → 回 `/login`
4. 未登录访问 `/dashboard` → 重定向 `/login?redirect=/dashboard`
5. 本地修改密码后请求 → 401 → 自动登出并跳登录页

- [ ] **步骤 5：手动验证清单（OAuth 配置缺失）**

1. `.env.development` 设 `VITE_AUTH_MODE=oauth`，保留 `VITE_OAUTH_ISSUER` 为空
2. `pnpm dev` → 应用启动报错（`createAuthProvider` 抛错），页面/控制台出现「OAuth 模式缺少配置」提示

- [ ] **步骤 6：手动验证清单（OAuth 正常流程，需可用 IdP）**

1. `.env.development` 设 `VITE_AUTH_MODE=oauth` 并填真实 `VITE_OAUTH_ISSUER`/`VITE_OAUTH_CLIENT_ID`（如 Keycloak），`VITE_BROKER_USERNAME/PASSWORD` 填 broker basic-auth 账号
2. 登录页显示「使用企业账号登录」→ 点击跳转 IdP → 授权 → 回跳 `/login?code=...&state=...`
3. 回调处理成功 → 顶栏显示 IdP 用户名
4. 浏览器 DevTools 确认：sessionStorage 中有 `mqtt_oauth_token`；后续 `/api/*` 请求携带 `Authorization: Basic`（broker 凭据），无 OAuth Bearer
5. 关闭浏览器标签页再打开 → 需重新登录（token 仅存 sessionStorage）
6. 退出登录 → sessionStorage 中 `mqtt_oauth_*` 被清除

---

## 自检记录

- **规格覆盖度：** 规格第 2 节架构（types/basic/oidc/index + store）→ 任务 1-5；第 3 节配置 → 任务 4（vite-env.d.ts）+ 任务 9（.env）；第 4 节 Basic 模式与改动点 → 任务 2、5、6；第 5 节 OAuth 流程/PKCE/端点发现/会话 → 任务 3；第 6 节数据流（axios 拦截器、401、路由守卫）→ 任务 6、7（守卫逻辑未变，仅 store 接口保持兼容）；第 7 节验证 → 任务 10。
- **路由守卫：** 规格第 6 节明确「路由守卫只判断 `authStore.isAuthenticated`，不感知模式」——现有 `router/index.ts` 的守卫已满足，无需改动（store 暴露的同名 ref 保持兼容）。
- **类型一致性：** `AuthProvider` 接口方法名（`init/isAuthenticated/getUsername/login/startLogin/handleCallback/getApiCredentials/logout`）在任务 1 定义、任务 2/3 实现、任务 5 store 调用、任务 8 Login.vue 使用，命名一致。
- **占位符扫描：** 无 TODO/待定/「适当处理」等占位表述；每个代码步骤含完整可粘贴代码。
